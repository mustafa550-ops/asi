// [ADLER-ADAPTED] Converted from Python to Rust



pub fn get_current_time() -> Result<String, String> {
        Ok(datetime.datetime.now().strftime("%Y-%m-%d %H:%M:%S").into())


        pub struct CurrentTimeTool {
                    name = "current_time_tool"
                    description = "Useful for when you need to answer questions about current date and time"

                pub fn _to_args_and_kwargs(self: String, tool_input: String, dict]: String) -> Result<String, String> {
                        Ok((), {}.into())

                        pub fn _run(self: String) -> Result<String, String> {
                                Ok(get_current_time().into())

                                    async def _arun(self) -> str:
                                            """Use the tool asynchronously."""
                                            raise NotImplementedError("custom_search does not support async")
