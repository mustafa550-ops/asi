echo "=== pre-commit checks (non-blocking) ==="
cargo clippy --workspace 2>/dev/null && echo "clippy OK" || echo "clippy: warnings (non-blocking)"
pnpm -r lint 2>/dev/null && echo "lint OK" || echo "lint: warnings (non-blocking)"
pnpm -r format 2>/dev/null && echo "format OK" || echo "format: warnings (non-blocking)"
